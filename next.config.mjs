/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['aceternity.com', 'assets.aceternity.com'], // Add other domains here if needed
    },
  };
  
  export default nextConfig;
  